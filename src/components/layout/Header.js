import * as React from "react"

const Header = (props) => {

    const style = {
      fontFamily: "'Courier New', Courier, monospace"
    }

    return (
        <h1 style={style}>magenta rapids</h1>
    )


}

export default Header;